[project]
name = "vibetuner-scaffolding"
version = "11.1.0"
requires-python = ">=3.11"

[tool.uv.sources]
vibetuner = { path = "vibetuner-py" }

[dependency-groups]
dev = [
    "lint-po>=0.1.5",
    "vibetuner[dev]",
]
