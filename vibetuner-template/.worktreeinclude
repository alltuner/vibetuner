# .worktreeinclude — local files Claude Code copies into new git worktrees.
# .gitignore syntax. Only gitignored files that match are copied; heavy,
# regenerable trees are deliberately left out (recreate them in the worktree
# with `uv sync` / `bun install`).
.env
.env.local
